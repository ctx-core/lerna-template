{
	"name": "@myproject/layout",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"build": "npm run clean && npm run compile",
		"clean": "rm -rf .rpt2_cache; find ./*/index.* -type f -not -name 'index.ts' -delete",
		"compile": "rollup -c",
		"prepublishOnly": "npm run build",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		"@ctx-core/fontawesome": "^10.2.35",
		"@ctx-core/nav": "^13.3.40",
		"@ctx-core/sapper": "^6.2.52",
		"@ctx-core/sass": "^6.3.16",
		"@ctx-core/theme": "^1.2.38",
		"@myproject/web-lib": "^0.0.1",
		"svelte": "^3.16.5"
	},
	"devDependencies": {
		"typescript": "^3.7.3"
	}
}